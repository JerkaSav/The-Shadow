import * as React from "react";

export const playboardBtns = (): React.ReactElement => (
  <div className="btn-options">
    <button
      onClick={() => {
        return;
      }}
    ></button>
    <button
      onClick={() => {
        return;
      }}
    ></button>
    <button
      onClick={() => {
        return;
      }}
    ></button>
  </div>
);
